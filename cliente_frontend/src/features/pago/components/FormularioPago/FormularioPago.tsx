import styles from "./FormularioPago.module.css"

type Props = {
    total: number,
    idUsuario: string
}


export default function FormularioPago({total, idUsuario}){

    return (
        <section>
            
            
        </section>
    )
}