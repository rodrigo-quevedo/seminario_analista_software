# Posicionamiento de componentes

El posicionamiento de cada componente se hace en la Page que los utiliza. Esto permite crear los estilos del componente de manera independiente a su posicionamiento. 

#### Así se logra que los estilos de posicionamiento/layout estén separados de los estilos del componente en sí mismo.